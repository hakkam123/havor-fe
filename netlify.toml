[build.environment]
  # These are public client-side API base values, not credentials.
  # Netlify scans values from public env vars in generated bundles, so omit these keys.
  SECRETS_SCAN_OMIT_KEYS = "VITE_API_BASE_URL,NUXT_PUBLIC_API_BASE"
